# dependencies
node_modules/
# build output
dist/
dist-ssr/
# tauri / rust
src-tauri/target/
src-tauri/gen/schemas/
release/
# env & secrets（ttls API key 只在 OS keychain；dev 用 .env.local，永不進版控）
.env
.env.local
.env.*.local
*.local
# editor
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
# logs
*.log
npm-debug.log*
# 使用者資料 / 樣本媒體 / 輸出
samples/**
!samples/README.md
outputs/
*.aicut.json
.aicut-cache/
# CLI bundle（npm run cli:build 產生）
dist-cli/
# 內建 ffmpeg（CI 用 scripts/fetch-ffmpeg.mjs 依 manifest 抓，138 MB 不進版控）
src-tauri/resources/ffmpeg/*
!src-tauri/resources/ffmpeg/README.txt
